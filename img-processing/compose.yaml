services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: img_processing
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d img_processing"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
